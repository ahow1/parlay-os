.worktrees/
.env
.env.save
*.db
*.db-shm
*.db-wal
__pycache__/
.pytest_cache/
data_cache/
*.log
*.swp
runlog.txt
sgo_cache.json

# Agent 2 (THE ANALYST)'s local institutional memory -- potentially
# sensitive pick data, not code. Railway's persistent filesystem keeps
# it safe across deploys; it must never land in the git-committed DB.
agent_memory/
